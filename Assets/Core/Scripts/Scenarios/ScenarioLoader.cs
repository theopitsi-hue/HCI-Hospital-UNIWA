public class ScenarioLoader
{
    //loads a json scenario and turns it (and its sub-objects) into a unity object.
    public ScenarioObject LoadScenario(string path)
    {
        return null;
    }

    //Saves a scenario unity object into a json
    public void SaveScenarioToFile(string path, ScenarioObject scenario)
    {
        
    }
}